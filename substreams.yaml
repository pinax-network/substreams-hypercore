specVersion: v0.1.0
package:
  name: hypercore
  version: v0.5.0
  url: https://github.com/pinax-network/substreams-hypercore
  description: ClickHouse sink for Hypercore
  image: ./image.png

imports:
  # DatabaseChanges output definitions. The ClickHouse sink owns its SQL import.
  database_changes: ./spkg/substreams-database-change-v2.0.0.spkg

protobuf:
  files:
    - block.proto
    - event.proto
    - fill.proto
  importPaths:
    - ./proto
  excludePaths:
    - sf/substreams
    - google

binaries:
  default:
    type: wasm/rust-v1
    file: ./target/wasm32-unknown-unknown/release/hypercore.wasm

modules:
  - name: map_fills
    kind: map
    inputs:
      - source: pinax.hypercore.v1.Block
    output:
      type: proto:pinax.hypercore.v1.Block

  - name: db_out
    kind: map
    inputs:
      - source: sf.substreams.v1.Clock
      - source: pinax.hypercore.v1.Block
    output:
      type: proto:sf.substreams.sink.database.v1.DatabaseChanges

network: hyprecore
